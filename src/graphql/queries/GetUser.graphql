query GetUser($login: String!) {
    user(login: $login) {
        login
        email
        avatarUrl
        name
        twitterUsername
        followers {
            totalCount
        }
        following {
            totalCount
        }
    }
}
